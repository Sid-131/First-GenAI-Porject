[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    integration: marks tests that require internet access and real HuggingFace data
    unit: marks pure unit tests that run without network or files

# Show short test summary, verbose output
addopts = -v --tb=short
