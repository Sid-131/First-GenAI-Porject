# Root-level requirements.txt for Streamlit Cloud deployment
# Streamlit Cloud runs pip install -r requirements.txt from the repo root.

# ── Streamlit ────────────────────────────────────────────────────────────────
streamlit>=1.35.0

# ── Backend dependencies (same as backend/requirements.txt) ──────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pandas>=2.0.0
datasets>=2.19.0
google-genai>=1.0.0
pydantic>=2.7.0
python-dotenv>=1.0.0
httpx>=0.27.0
